<UserControl x:Class="AzureVmScriptRunner.UI.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:AzureVmScriptRunner.UI.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PlaceholderViewModel}">

  <Border Style="{StaticResource Panel.Banner}" Margin="24" VerticalAlignment="Top">
    <StackPanel>
      <TextBlock Text="{Binding Title}" Style="{StaticResource Text.SectionHeader}" FontSize="14" Margin="0,0,0,6"/>
      <TextBlock Text="{Binding Note}" TextWrapping="Wrap" Foreground="{StaticResource Brush.TextMuted}"/>
    </StackPanel>
  </Border>
</UserControl>
